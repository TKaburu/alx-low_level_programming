#include <stdio.h>
/*
 * main - Entry point
 * Description: Prints the alphabet in lowercase, and then in uppercase.
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 97
	int o = 65

	while (n <= 122)
	{
		putchar(n);
		n++
	}
	while (o <= 90)
	{
		putcharr(o);
		o++
	}
	return (0)
}
