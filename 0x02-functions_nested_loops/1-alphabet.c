#include <stdio.h>
/**
 * main - Entry point
 * Description: a function that prints the alphabet, in lowercase
 * return: Always (0)
 */
void print_alphabet(void)
{
	char letter;

	letter = 'a';
	while (a <= 'z')
	{
		putchar(letter);
		letter++
	}
}

int main(void)
{
	ft_print_alphabet();
	return (0);
}
