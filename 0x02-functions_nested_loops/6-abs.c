#include "main.h"
/**
 * _abs - Gives the abolute value
 * of an interger
 *
 * @: inputs number as an interger
 *
 * Return absolute value
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (a * -1);
	}
}
